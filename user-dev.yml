spring:
  datasource:
      driver-class-name: com.mysql.jdbc.Driver
      username: root
      password: root
      url: jdbc:mysql://localhost/spring_cloud?characterEncoding=utf-8&useSSL=false
  redis:
    host: localhost
    port: 6379
